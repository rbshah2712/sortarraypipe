import { SortarraypipePipe } from './sortarraypipe.pipe';

describe('SortarraypipePipe', () => {
  it('create an instance', () => {
    const pipe = new SortarraypipePipe();
    expect(pipe).toBeTruthy();
  });
});
